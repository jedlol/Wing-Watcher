<h1>User Items</h1>
<table class="table table-hover">
  <thead class="thead-dark">
    <tr>
      <th>Image</th>
    </tr>
  </thead>
  <tbody>
    <% @users.each do |user| %>
      <tr>
    	<td><%= image_tag(user.image_url)%></td>
      </tr>
    <% end %>
  </tbody>
</table>